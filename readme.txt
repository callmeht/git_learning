this is a test file.
git checkout -- readme.txt  this goes back to the last commit from git add or not git add.
git reset HEAD~n. this goes back to the n_th before now version.
git reset HEAD^. this goes back to the last version.
git reset -- hard (log). this goes to the log version.

